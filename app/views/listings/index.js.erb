$boxes = $('<%= j render(@listings) %>')

$('#listings').append( $boxes ).imagesLoaded( function(){
    $('#listings').masonry( 'reload');
});
<% if @listings.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@listings) %>');
<% else %>
$('.pagination').remove();
<% end %>